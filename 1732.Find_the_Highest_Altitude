class Solution
{
public:
    int largestAltitude(vector<int> &gain)
    {
        int maxo=0,sum=0;
        for(auto x:gain){
            sum+=x;
            maxo=max(maxo,sum);
        }
        return maxo;
    }
};
